Each barcode is defined as an image and added to the resulting table.
It is possible to adjust the extension of the final image in the source code, but in this case you will have to rebuild the binary file.
The binary file is located in the dist folder, for it to work correctly you need to keep them together with barcodes_icon.iso